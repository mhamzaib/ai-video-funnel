# Legacy single-file state (pre series-pack architecture).

Active series live under `series/<slug>/`.
VOID_SIGNAL was migrated to `series/void_signal/`.

You can delete this folder once you no longer need the old JSON.
